import React from 'react';

const UserPanel = () => {
        return(
            <section className="UserPanel">
                UserPanel
            </section>
        );
}

export default UserPanel;