import UserProfile from "./userProfile/UserProfile";

const UserPanel = () => {

        return(
            <section className="userPanel">
                <UserProfile/>
            </section>
        );
}

export default UserPanel;


